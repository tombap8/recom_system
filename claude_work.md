# 🎬 영화 추천 시스템 프로젝트 기획서

> 참고 도서: 『파이썬 머신러닝 완벽 가이드』 9장 - 추천 시스템
> 목적: Study 프로젝트 (발표 자료 + 시연 가능한 웹 UI 포함)

---

## 1. 프로젝트 개요

책의 9장(추천 시스템) 중 **콘텐츠 기반 필터링(장르 기반)**을 활용한 TMDB 영화 추천 시스템을 구현한다.
단순 텍스트 출력이 아니라 **포스터 이미지 + 추천 근거를 시각적으로 보여주는 웹 UI**까지 만들어
"학습한 티가 나면서도 상용 서비스처럼 보이는" 프로젝트로 완성하는 것이 목표.

추후 상황에 따라 아래 2개 프로젝트를 추가할지 결정:
- 캐글 주택 가격 예측 (5장 회귀) — 시연: 입력값 조정 → 예상 가격 출력
- 캐글 신용카드 사기 검출 (4장 분류) — 시연: 거래 정보 입력 → 사기 확률 출력

---

## 2. 프로젝트 구조

```
movie-recommender/
├── data/
│   └── tmdb_5000_movies.csv       # 캐글 TMDB 5000 Movie Dataset
├── notebook/
│   └── movie_recommender.ipynb    # 분석 + 모델 개발 과정 (발표용 스토리)
├── app/
│   └── recommender_ui.html        # 시연용 웹 UI (Claude Artifact)
└── README.md                      # 프로젝트 개요, 결과 요약
```

---

## 3. 데이터셋 정보

- **이름**: TMDB 5000 Movie Dataset (Kaggle)
- **영화 수**: 약 4,800개
- **파일**:
  - `tmdb_5000_movies.csv` — 제목, 장르, 줄거리, 평점(`vote_average`), 투표수(`vote_count`), 개봉일, 예산 등
  - `tmdb_5000_credits.csv` — 감독, 배우진 정보 (이번 프로젝트에서는 선택적으로만 사용)
- **용량**: 약 5~10MB
- **주의사항**: 포스터 이미지는 CSV에 `poster_path`로만 들어있음.
  실제 이미지 표시를 위해서는 TMDB 이미지 URL 조합 필요:
  ```
  https://image.tmdb.org/t/p/w500/{poster_path}
  ```
  → TMDB API 키 발급 필요 여부 확인할 것

---

## 4. 구현 단계 (책 목차 기준)

| 단계 | 내용 | 책 참고 위치 |
|---|---|---|
| 1 | TMDB 5000 데이터 로딩 및 가공 | 9장 - 데이터 로딩 및 가공 |
| 2 | 장르 텍스트 벡터화 (CountVectorizer) | 9장 - 장르 콘텐츠 유사도 측정 |
| 3 | 코사인 유사도 행렬 계산 | 9장 - 장르 콘텐츠 유사도 측정 |
| 4 | 영화 제목 입력 → 유사 영화 추천 함수 구현 | 9장 - 장르 콘텐츠 필터링을 이용한 영화 추천 |
| 5 | 가중 평점(Weighted Rating) 반영해서 추천 품질 개선 | 9장 실전 팁 |
| 6 | Before/After 비교 로직 (순수 유사도 vs 가중 평점 반영) | - |
| 7 | 웹 UI로 시연 가능하게 포장 | Claude Artifact |

---

## 5. 핵심 알고리즘 메모

### 5-1. 코사인 유사도 기반 추천
- 장르를 원-핫/CountVectorizer로 벡터화
- 벡터 간 코사인 유사도 계산 → 유사도 높은 순으로 추천

### 5-2. 가중 평점 공식 (IMDB 방식)
```
가중 평점 = (v / (v+m)) × R + (m / (v+m)) × C

v = 해당 영화의 투표 수 (vote_count)
m = 추천 리스트에 포함되기 위한 최소 투표 수 기준치
R = 해당 영화의 평균 평점 (vote_average)
C = 전체 영화의 평균 평점
```
- 초보자용 쉬운 설명: "평가한 사람이 적은 영화는 평점을 곧이곧대로 믿지 않고, 전체 평균 쪽으로 살짝 당겨서 계산해요"

---

## 6. 시연 UI 화면 구성

### 상단: 영화 검색 + 포스터
- 영화 제목 입력 → 선택 영화의 포스터/줄거리 표시

### 중단: 추천 결과 (포스터 카드 그리드)
각 카드에 표시할 정보:
- 포스터 이미지
- 유사도 점수 (%)
- 공통 장르 하이라이트 태그

### "왜 이렇게 추천됐을까?" 설명 패널 (초보자용 스토리텔링)
```
📌 어떻게 추천했나요?

1단계: 영화를 숫자로 바꿨어요
   "다크 나이트"의 장르는 [액션, 범죄, 드라마]
   → 컴퓨터는 이걸 [1, 1, 1, 0, 0, 0...] 같은 숫자로 이해해요

2단계: 숫자들이 얼마나 비슷한지 계산했어요
   "배트맨 비긴즈"의 장르 숫자와 비교해보니
   → 87% 비슷하다는 결과가 나왔어요 (코사인 유사도)

3단계: 그런데 비슷하다고 다 좋은 영화는 아니겠죠?
   평점이 낮은 영화가 섞여있을 수 있어서
   → 평점도 같이 고려해서 순위를 다시 매겼어요 (가중 평점)
```

### Before / After 비교 섹션
- 왼쪽: 순수 유사도 순위만 적용한 추천 리스트
- 오른쪽: 가중 평점까지 반영한 추천 리스트
- 순위 변동은 화살표(⬆️⬇️)로 표시
- 순위가 바뀐 이유를 한 줄 설명 추가 (예: "A 영화는 장르는 비슷했지만 평점이 낮아서 순위가 내려갔어요")

### (선택) 벡터 표현 테이블
- 장르 원-핫 벡터를 표로 보여줘서 "코사인 유사도로 각도를 계산했다"는 개념을 시각적으로 설명

---

## 7. 발표 자료 vs 시연 UI 역할 분담

| 구분 | 담을 내용 |
|---|---|
| **발표 노트북** (기술적 증명) | 벡터 표현 원리, 코사인 유사도 계산 과정, Before/After 비교 근거, 전체 파이프라인 코드 |
| **시연 UI** (직관적 임팩트) | 포스터 이미지, 유사도 %, 장르 하이라이트, 가중 평점 Before/After, 쉬운 설명 패널 |

---

## 8. 다음 액션 아이템 (VSCode 작업 시작점)

- [ ] Kaggle에서 `tmdb_5000_movies.csv` 다운로드 후 `data/` 폴더에 저장
- [ ] 데이터 구조 확인 (컬럼명, 결측치, `poster_path` 형식 등)
- [ ] TMDB API 키 발급 필요 여부 결정 (포스터 이미지 실제 표시용)
- [ ] `notebook/movie_recommender.ipynb` 생성 후 1~6단계 순서로 구현
- [ ] 유사도 계산 + 가중 평점 로직 함수화
- [ ] 웹 UI(`app/recommender_ui.html`) 설계 — Claude Artifact로 제작
- [ ] Before/After 비교 로직을 UI에 연결
- [ ] README.md 작성 (프로젝트 개요, 실행 방법, 스크린샷)

---

## 9. 추가 검토 예정 사항

- 포스터 이미지 소스: TMDB API vs 로컬 이미지 캐싱 방식 결정 필요
- 가중 평점의 최소 투표 수 기준치(m) 설정값 결정 (책 예시 값 참고 또는 분위수 기반 설정)
- 추후 2번(주택 가격), 1번(사기 검출) 프로젝트 진행 여부는 이 프로젝트 완료 후 결정
